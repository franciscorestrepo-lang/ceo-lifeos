export default async(req)=>{if(req.method!=='POST')return new Response('Method not allowed',{status:405});try{const body=await req.json();const m=body.microsoft||{};return Response.json({ok:true,counts:{inbox:m.inbox?.length||0,sent:m.sent?.length||0,flagged:m.flagged?.length||0,events:m.events?.length||0,teams:m.chatMessages?.length||0,read:body.read?.meetings?.length||0},health:m.health||{}})}catch(e){return Response.json({error:e.message},{status:500})}}
