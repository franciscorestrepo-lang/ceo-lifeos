import { readFileSync,readdirSync,statSync } from 'node:fs';import { join } from 'node:path';const required=['index.html','app.js','styles.css','manifest.webmanifest','sw.js','netlify.toml','data/current.json'];for(const f of required)readFileSync(f,'utf8');JSON.parse(readFileSync('data/current.json','utf8'));function walk(d){for(const x of readdirSync(d)){const p=join(d,x);if(statSync(p).isDirectory())walk(p);else if(p.endsWith('.json'))JSON.parse(readFileSync(p,'utf8'))}}walk('data');console.log('CEO LifeOS validation OK');
